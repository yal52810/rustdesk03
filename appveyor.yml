version: 1.0.{build}
image: Visual Studio 2022
build: off

test_script:
  - ps: |
      Write-Host "appveyor.yml IS working!"
      Write-Host "RustDesk build test"
      rustc --version 2>$null; Write-Host "Rust not installed yet (expected)"
